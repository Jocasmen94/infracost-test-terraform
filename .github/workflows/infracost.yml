name: Infracost CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:  # Permite ejecución manual

jobs:
  infracost:
    runs-on: ubuntu-latest
    name: Infracost Diff
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Debug Start
        run: |
          echo "Job started at $(date)"
          echo "Runner OS: $(uname -a)"
          echo "Event Name: ${{ github.event_name }}"
          echo "Event Action: ${{ github.event.action }}"
          echo "PR User: ${{ github.event.pull_request.user.login }}"
          echo "GitHub Context: ${{ toJson(github) }}"

      - name: Setup Infracost
        uses: infracost/actions/setup@v2
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      # Checkout PR branch first
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      # Debug repository structure
      - name: List repository files
        run: |
          pwd  # Muestra el directorio de trabajo actual
          ls -R .  # Lista todos los archivos desde el directorio actual
          echo "Current directory contents: $(ls -a)"

      # Detect config file path with fixed location
      - name: Detect config file path
        id: detect
        run: |
          CONFIG_PATH="workspaces/kubernetes/prod/main/infracost.yml"
          if [ -f "$CONFIG_PATH" ]; then
            echo "CONFIG_PATH=$CONFIG_PATH" >> $GITHUB_ENV
            echo "Detected Infracost config at: $CONFIG_PATH"
          else
            echo "Infracost.yml not found at $CONFIG_PATH in PR branch. Exiting..."
            exit 1
          fi

      # Extract path from infracost.yml
      - name: Extract project path
        id: extract_path
        run: |
          PROJECT_PATH=$(yq eval '.projects[0].path' "$CONFIG_PATH")
          echo "PROJECT_PATH=$PROJECT_PATH" >> $GITHUB_ENV
          echo "Extracted project path: $PROJECT_PATH"

      # Checkout base branch
      - name: Checkout base branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.ref }}

      - name: Generate Infracost baseline
        run: |
          if [ -f "$CONFIG_PATH" ]; then
            infracost breakdown \
              --path="$PROJECT_PATH" \
              --config-file="$CONFIG_PATH" \
              --format=json \
              --out-file=/tmp/infracost-base.json
          else
            echo "Missing config at $CONFIG_PATH in base branch"; exit 1
          fi

      # Checkout PR branch again
      - name: Checkout PR branch again
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Generate Infracost diff
        run: |
          infracost diff \
            --path="$PROJECT_PATH" \
            --config-file="$CONFIG_PATH" \
            --format=json \
            --compare-to=/tmp/infracost-base.json \
            --out-file=/tmp/infracost.json

      - name: Post Infracost comment
        run: |
          infracost comment github \
            --path=/tmp/infracost.json \
            --repo=$GITHUB_REPOSITORY \
            --github-token=${{ github.token }} \
            --pull-request=${{ github.event.pull_request.number }} \
            --behavior=update

      - name: Debug End
        run: |
          echo "Job completed at $(date)"